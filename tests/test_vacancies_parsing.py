#!/usr/bin/env python3
"""
Юнит-тесты для функции parse_vacancies_direct
"""

import sys
import os
sys.path.append(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from routes.vacancies import parse_vacancies_direct
import json

def test_correct_column_indices():
    """Тест правильности индексов колонок"""
    print("🧪 Тестируем правильность индексов колонок...")
    
    test_csv = """Акции и скидки,Объект,Оплата,Условия,Требования,Потребность,Менеджер,Юр.лицо,Преимущества
Должность:,Объект,Оплата,Условия,Требования,Потребность,Менеджер,Юр.лицо,Преимущества
,Сборщик на конвейере,320 руб/час,смена - 11 часов,От 18 до 45 лет,15 муж.,Константин Тренин,ООО Фортренд,Сборка бытовой техники"""
    
    result = parse_vacancies_direct(test_csv)
    
    assert len(result) == 1, f"Ожидалось 1 вакансия, получено {len(result)}"
    
    vacancy = result[0]
    
    # Проверяем правильность индексов
    assert vacancy['position'] == 'Сборщик на конвейере', f"Неправильная должность: {vacancy['position']}"
    assert vacancy['salary'] == '320 руб/час', f"Неправильная зарплата: {vacancy['salary']}"
    assert vacancy['conditions'] == 'смена - 11 часов', f"Неправильные условия: {vacancy['conditions']}"
    assert vacancy['requirements'] == 'От 18 до 45 лет', f"Неправильные требования: {vacancy['requirements']}"
    assert vacancy['positions_needed'] == '15 муж.', f"Неправильная потребность: {vacancy['positions_needed']}"
    assert vacancy['manager'] == 'Константин Тренин', f"Неправильный менеджер: {vacancy['manager']}"
    assert vacancy['company'] == 'ООО Фортренд', f"Неправильная компания: {vacancy['company']}"
    assert vacancy['benefits'] == 'Сборка бытовой техники', f"Неправильные преимущества: {vacancy['benefits']}"
    
    print("✅ Индексы колонок правильные!")

def test_header_skipping():
    """Тест пропуска заголовков"""
    print("🧪 Тестируем пропуск заголовков...")
    
    test_csv = """Акции и скидки,Объект,Оплата,Условия,Требования,Потребность,Менеджер,Юр.лицо,Преимущества
Должность:,Объект,Оплата,Условия,Требования,Потребность,Менеджер,Юр.лицо,Преимущества
,Сборщик на конвейере,320 руб/час,смена - 11 часов,От 18 до 45 лет,15 муж.,Константин Тренин,ООО Фортренд,Сборка бытовой техники
,Работник склада,370 руб/час,смена - 11 часов,РФ/РБ,0,Виктор Горяинов,ООО Фортренд,Брендовая одежда"""
    
    result = parse_vacancies_direct(test_csv)
    
    assert len(result) == 2, f"Ожидалось 2 вакансии, получено {len(result)}"
    
    # Проверяем, что заголовки не попали в результат
    positions = [v['position'] for v in result]
    assert 'Объект' not in positions, "Заголовок попал в результат!"
    assert 'Должность:' not in positions, "Заголовок попал в результат!"
    
    print("✅ Заголовки правильно пропускаются!")

def test_empty_fields():
    """Тест обработки пустых полей"""
    print("🧪 Тестируем обработку пустых полей...")
    
    test_csv = """Акции и скидки,Объект,Оплата,Условия,Требования,Потребность,Менеджер,Юр.лицо,Преимущества
Должность:,Объект,Оплата,Условия,Требования,Потребность,Менеджер,Юр.лицо,Преимущества
,Водитель,,смена - 12 часов,Водительские права,5 чел.,Анна Смирнова,,"""
    
    result = parse_vacancies_direct(test_csv)
    
    assert len(result) == 1, f"Ожидалось 1 вакансия, получено {len(result)}"
    
    vacancy = result[0]
    assert vacancy['position'] == 'Водитель'
    assert vacancy['salary'] == '', "Пустая зарплата должна быть пустой строкой"
    assert vacancy['benefits'] == '', "Пустые преимущества должны быть пустой строкой"
    
    print("✅ Пустые поля обрабатываются правильно!")

def test_unicode_and_special_chars():
    """Тест спецсимволов и unicode"""
    print("🧪 Тестируем спецсимволы и unicode...")
    
    test_csv = """Акции и скидки,Объект,Оплата,Условия,Требования,Потребность,Менеджер,Юр.лицо,Преимущества
Должность:,Объект,Оплата,Условия,Требования,Потребность,Менеджер,Юр.лицо,Преимущества
,Менеджер по продажам,50000 руб/мес,офис,Высшее образование,3 чел.,Сергей Иванов,ООО Технологии,Карьерный рост & развитие"""
    
    result = parse_vacancies_direct(test_csv)
    
    assert len(result) == 1, f"Ожидалось 1 вакансия, получено {len(result)}"
    
    vacancy = result[0]
    assert '&' in vacancy['benefits'], "Спецсимволы должны сохраняться"
    assert 'ООО Технологии' in vacancy['company'], "Название компании должно сохраняться"
    
    print("✅ Спецсимволы и unicode обрабатываются правильно!")

def test_validation():
    """Тест валидации данных"""
    print("🧪 Тестируем валидацию данных...")
    
    # Тест с неправильными данными
    test_csv_bad = """Акции и скидки,Объект,Оплата,Условия,Требования,Потребность,Менеджер,Юр.лицо,Преимущества
Должность:,Объект,Оплата,Условия,Требования,Потребность,Менеджер,Юр.лицо,Преимущества
,А,нет_чисел,смена - 11 часов,От 18 до 45 лет,нет_чисел,Константин Тренин,ООО Фортренд,Сборка бытовой техники"""
    
    result_bad = parse_vacancies_direct(test_csv_bad)
    
    # Валидация должна отфильтровать неправильные данные
    assert len(result_bad) == 0, f"Неправильные данные должны быть отфильтрованы, получено {len(result_bad)}"
    
    # Тест с правильными данными
    test_csv_good = """Акции и скидки,Объект,Оплата,Условия,Требования,Потребность,Менеджер,Юр.лицо,Преимущества
Должность:,Объект,Оплата,Условия,Требования,Потребность,Менеджер,Юр.лицо,Преимущества
,Сборщик на конвейере,320 руб/час,смена - 11 часов,От 18 до 45 лет,15 муж.,Константин Тренин,ООО Фортренд,Сборка бытовой техники"""
    
    result_good = parse_vacancies_direct(test_csv_good)
    
    assert len(result_good) == 1, f"Правильные данные должны проходить валидацию, получено {len(result_good)}"
    
    print("✅ Валидация работает правильно!")

def test_large_file():
    """Тест большого файла"""
    print("🧪 Тестируем большой файл...")
    
    # Создаем CSV с 20 вакансиями
    header = "Акции и скидки,Объект,Оплата,Условия,Требования,Потребность,Менеджер,Юр.лицо,Преимущества\nДолжность:,Объект,Оплата,Условия,Требования,Потребность,Менеджер,Юр.лицо,Преимущества\n"
    
    rows = []
    for i in range(20):
        row = f",Вакансия {i+1},{(i+1)*100} руб/час,смена - 8 часов,Опыт работы,{i+1} чел.,Менеджер {i+1},Компания {i+1},Преимущества {i+1}\n"
        rows.append(row)
    
    test_csv = header + "".join(rows)
    
    result = parse_vacancies_direct(test_csv)
    
    assert len(result) == 20, f"Ожидалось 20 вакансий, получено {len(result)}"
    
    # Проверяем, что все вакансии уникальны
    positions = [v['position'] for v in result]
    assert len(set(positions)) == 20, "Не все вакансии уникальны"
    
    print("✅ Большой файл обрабатывается правильно!")

def run_all_tests():
    """Запуск всех тестов"""
    print("🚀 Запускаем все тесты для parse_vacancies_direct...")
    
    try:
        test_correct_column_indices()
        test_header_skipping()
        test_empty_fields()
        test_unicode_and_special_chars()
        test_validation()
        test_large_file()
        
        print("\n🎉 Все тесты прошли успешно!")
        return True
        
    except Exception as e:
        print(f"\n❌ Тест провален: {e}")
        import traceback
        traceback.print_exc()
        return False

if __name__ == "__main__":
    success = run_all_tests()
    exit(0 if success else 1)
